// Central production URL — override with NEXT_PUBLIC_SITE_URL at build time
// if the deployed domain differs. Used by metadataBase, OG URLs, robots, sitemap.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://ekoboy2men.org";